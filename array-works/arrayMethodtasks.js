
//create a new array contains of all 

var words = ["carrot","beetroot","parrot","apple"]

// var wordLength = words.map(w=>w.length);
// console.log(wordLength);

// all elements in upper case
// var uppercase=words.map(w=>w.toUpperCase())
// console.log(uppercase);

//rr words
// var rrwords = words.filter(w=>w.includes("rr"));
// console.log(rrwords);

//longest word

// var longest = words.reduce((w1,w2)=>w1.length>w2.length?w1:w2)
// console.log(longest);

// var sort = words.sort((w1,w2)=>w2.length-w1.length)
// console.log(sort);

